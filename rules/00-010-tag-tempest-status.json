{
	"conditions": {
		"check-tempest-event": " '${body.serial_number}' === '${tempest-device-serial}' ",
        "check-event-type": " '${body.type}' === 'device_status' "
	},
	"actions": {
		"action-tag": { 
			"type": "tag", 
			"value": "tempest - status",
			"expire": 1
		},
		"add label: tempest-status": {
			"type": "add-label",
			"label": "tempest-status"
		},
		"filter: only tempest": {
			"type": "rule-filter",
			"match": ".*tempest.*"
		}
	}
}
