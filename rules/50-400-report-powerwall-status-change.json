{
	"labels": [ "powerwall" ],
	"conditions": {
		"check-pw-instancee": " [ '9NK', 'BBW' ].includes( '${body.instance}' ) ",
		"monitored-endpoint": " '${body.endpoint}' === 'control-mode' ",
		"enabled": true
	},
	"actions": {
		"post-rule email action": {
			"type": "endpoint",
			"endpoint": "${url_base_post_rule_actions}",
			"method": "POST",
			"body": {
				"action": "email",
				"email_subject": "Powerwall ${body.instance} mode change",
				"email_body": "Powerwall mode change\nSystem: ${body.instance}\nEvent-type: ${body.event-type}\nEvent: ${event_id}\nGrid status: ${Electric.grid.value}\n\nCurrent value\n    (raw): ${body.current}\n    mode: ${body.current.mode}\n    error: ${body.current.error}",
				"email_attachments": ""
			}
		}
	}
}
