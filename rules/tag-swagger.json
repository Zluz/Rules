{
	"conditions": {
		"check-referer": "'${headers.referer}' == '${rule-proc-url-base}/docs'",
		"check-origin": "'${headers.origin}' == '${rule-proc-url-base}'"
	},
	"actions": {
		"action-tag": { 
			"type": "tag", 
			"value": "swagger"
		}
	}
}
